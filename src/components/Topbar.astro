---
interface Props {
    buttons?: ("home" | "essays")[];
}

const { buttons = ["home", "essays"] } = Astro.props;
---

<style>
    #topbar {
        display: flex;
        justify-content: left;
        align-items: center;
        gap:50px
    }
    
</style>

<div id="topbar" >
    {buttons.includes("home") && (
        <a class="topbar-button" href="/hello">
            <h1> > ~ </h1>
        </a>
    )}
    {buttons.includes("essays") && (
        <a class="topbar-button" href="/essays">
            <h1> essays </h1>
        </a>
    )}
</div>